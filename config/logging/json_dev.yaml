# JSON lines, verbose level for local debugging.
version: 1
disable_existing_loggers: false
formatters:
  json:
    (): app.logging_setup.JsonLogFormatter
handlers:
  stderr:
    class: logging.StreamHandler
    formatter: json
    stream: ext://sys.stderr
root:
  level: DEBUG
  handlers: [stderr]
