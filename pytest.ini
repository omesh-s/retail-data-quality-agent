[pytest]
# Repo-root packages (config, myagent, app) — same layout as evaluate.py / run_day.py.
pythonpath = .
testpaths = tests
# MCP-first active test scope. Legacy local-pipeline tests are quarantined
# and intentionally excluded from default runs in Phase B.
python_files =
    test_adk_mcp_integration.py
    test_retail_tool.py
    test_anomaly_to_prompt.py
    test_settings.py
    test_mcp_stdio_client.py
    test_mcp_sse_client.py
    test_service_health.py
